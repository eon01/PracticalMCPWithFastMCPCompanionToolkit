# Async vs Sync Tools: When Each One Wins


```python
def dog_to_human_age(age: int) -> int:
    """Calculate the real age of a dog in human years."""
    return age * 7
```


```python
import asyncio

@mcp.tool
async def dog_to_human_age(age: int) -> int:
    """Calculate the real age of a dog in human years."""
    # Simulate an asynchronous operation (e.g., a database query or API call)
    await asyncio.sleep(1)  # Simulating a delay
    return age * 7
```
